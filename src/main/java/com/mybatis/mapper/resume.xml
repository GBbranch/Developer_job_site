<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Resume">
	<insert id="write" parameterType="com.dto.ResumeDTO">
		INSERT INTO RESUME VALUES
		(resume_seq.NEXTVAL, #{user_id} , #{career} , #{portfolio}
	</insert>
	<insert id="insertSchool" parameterType="com.dto.SchoolDTO">
		INSERT INTO school (id,member_id, Rating, name, department, status, grade)
		VALUES
		(school_seq.NEXTVAL, #{member_id}, #{Rating}, #{name},
		#{department},#{status}, #{grade})
	</insert>
	<insert id="insertLicence" parameterType="com.dto.LicenceDTO">
		INSERT INTO licence (id, resume_id, title)
		VALUES (licence_seq.NEXTVAL, #{resume_id}, #{title})
	</insert>
	<insert id="reg" parameterType="com.dto.RegDTO">
	
			
	</insert>

</mapper>